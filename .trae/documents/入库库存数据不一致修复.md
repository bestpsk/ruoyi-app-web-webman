# 入库表和库存表数据不一致问题修复

## 问题现象

从截图可见：
- **入库单 RK20260504001**：总数量 **10盒（100支）**，状态已确认
- **库存表 GCS-p7**：当前库存 **0盒（0支）**，预警状态

## 根因分析

### 问题定位：confirmStockIn 存在**双重转换 bug**

**insertStockIn（保存时）** - [BizStockInService.php#L108-L110](file:///d:\fuchenpro\webman\app\service\BizStockInService.php#L108-L110)：
```php
if ($unitType === '1' && $packQty > 1) {
    $item['original_quantity'] = intval($item['quantity']);  // 原始数量 = 10
    $item['quantity'] = intval($item['quantity']) * $packQty; // 已转换为最小单位 = 10*10=100
}
```
→ 数据库存储的 `quantity = 100`（最小单位）

**confirmStockIn（确认时）** - [BizStockInService.php#L208-L213](file:///d:\fuchenpro\webman\app\service\BizStockInService.php#L208-L213)：
```php
$packQty = $product->pack_qty ?? 1;       // pack_qty = 10
$unitType = $item->unit_type ?? '1';      // unit_type = '1'
$actualQty = $item->quantity;             // 从数据库读取 = 100
if ($unitType === '1') {
    $actualQty = $item->quantity * $packQty; // 又乘了一次！= 100*10 = 1000 ❌
}
```

**结果**：库存增加了 1000 而不是 100！或者如果因为其他原因导致异常，可能根本没有正确更新。

### 对比 confirmStockOut（出库确认）— 正确的实现

[BizStockOutService.php#L206](file:///d:\fuchenpro\webman\app\service\BizStockOutService.php#L206)：
```php
$actualQty = intval($item->quantity);  // 直接使用数据库中的值，没有额外转换 ✅
```

## 修复方案

### 修改 confirmStockIn 方法

删除多余的单位转换逻辑，直接使用数据库中已转换的 `quantity`：

```php
public function confirmStockIn($stockInId)
{
    $stockIn = BizStockIn::find($stockInId);
    if (!$stockIn) {
        return ['success' => false, 'msg' => '入库单不存在'];
    }
    if ($stockIn->status === '1') {
        return ['success' => false, 'msg' => '入库单已确认，请勿重复操作'];
    }
    $items = BizStockInItem::where('stock_in_id', $stockInId)->get();
    if ($items->isEmpty()) {
        return ['success' => false, 'msg' => '入库单明细为空'];
    }
    foreach ($items as $item) {
        $inventory = BizInventory::where('product_id', $item->product_id)->first();
        if (!$inventory) {
            $product = BizProduct::find($item->product_id);
            $inventory = BizInventory::create([
                'product_id' => $item->product_id,
                'quantity' => 0,
                'warn_qty' => $product ? ($product->warn_qty ?? 0) : 0,
                'create_time' => date('Y-m-d H:i:s'),
            ]);
        }
        $actualQty = intval($item->quantity);  // 直接使用，不再转换
        $inventory->quantity = $inventory->quantity + $actualQty;
        $inventory->last_stock_in_time = date('Y-m-d H:i:s');
        $inventory->update_time = date('Y-m-d H:i:s');
        $inventory->save();
    }
    BizStockIn::where('stock_in_id', $stockInId)->update([
        'status' => '1',
        'update_time' => date('Y-m-d H:i:s'),
    ]);
    return ['success' => true, 'msg' => '入库确认成功'];
}
```

## 文件修改清单

| 文件 | 修改内容 |
|------|----------|
| `webman/app/service/BizStockInService.php` | confirmStockIn 方法移除双重转换逻辑 |

## 验证步骤

1. 重启 webman 后端
2. 清除现有测试数据（或手动修正库存）
3. 新建入库单 → 确认 → 验证库存是否正确更新
4. 入库10盒（1盒=10支）→ 库存应显示 10盒（100支）
