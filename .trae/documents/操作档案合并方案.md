# 操作档案合并方案

## 📋 问题描述

**现象**：一次性操作两个项目时，会生成两条独立的客户档案记录

**截图示例**（同一客户、同一天的操作）：
```
2026-05-09  操作  销售  admin
方案：品项2×1          ← 第一条档案
金额：¥398.00  满意度：⭐⭐⭐⭐⭐

2026-05-09  操作  销售  admin
方案：品项1×1          ← 第二条档案（应该合并到上面）
金额：¥398.00  满意度：⭐⭐⭐⭐⭐
```

**期望结果**：合并为一条档案记录显示
```
2026-05-09  操作  销售  admin
方案：品项2×1, 品项1×1   ← 合并后的档案
金额：¥796.00  满意度：⭐⭐⭐⭐⭐
```

---

## 🔍 问题根因分析

### 当前代码流程

```
用户选择2个项目 → 前端提交2次请求（或后端处理2次）
    ↓
第1次调用 BizOperationRecordService.insertRecord(项目A)
    ├── 创建 biz_operation_record (record_id=101, product_name="品项2")
    └── 调用 insertArchiveFromOperation(record_101)
        └── 创建 biz_customer_archive (archive_id=501, source_id=101) ✅ 第1条档案

第2次调用 BizOperationRecordService.insertRecord(项目B)
    ├── 创建 biz_operation_record (record_id=102, product_name="品项1")
    └── 调用 insertArchiveFromOperation(record_102)
        └── 创建 biz_customer_archive (archive_id=502, source_id=102) ✅ 第2条档案 ❌ 应该合并！
```

### 当前去重逻辑（[BizCustomerArchiveService.php:95-96](file:///d:\fuchenpro\webman\app\service\BizCustomerArchiveService.php#L95-L96)）

```php
// 只检查同一条操作记录是否已生成档案
$exists = BizCustomerArchive::where('source_type', '1')
    ->where('source_id', $recordId)
    ->first();
if ($exists) return $exists;
```

**问题**：去重条件是 `source_id = recordId`，不同操作记录的 `record_id` 不同，所以不会触发合并！

---

## ✅ 解决方案：按"同客户+同日期"合并档案

### 核心思路

修改 `insertArchiveFromOperation()` 方法：
- **创建前检查**：同一客户 + 同一天 + 已有操作档案？
- **如果有**：更新该档案（合并plan_items、累加金额、合并照片等）
- **如果没有**：正常创建新档案

### 合并规则

| 字段 | 合并策略 |
|------|---------|
| `plan_items` | JSON数组追加新项目 |
| `amount` | 累加金额 |
| `photos` | 数组合并去重 |
| `customer_feedback` | 取最新值 |
| `satisfaction` | 取最新值 |
| `remark` | 追加拼接 |
| `source_id` | 更新为最新的record_id |

---

## 📝 具体修改步骤

### 步骤1：修改 [BizCustomerArchiveService.php](file:///d:\fuchenpro\webman\app\service\BizCustomerArchiveService.php)

**文件位置**：`d:\fuchenpro\webman\app\service\BizCustomerArchiveService.php`  
**方法**：`insertArchiveFromOperation()` （第89-143行）

#### 修改前（当前代码）：
```php
public function insertArchiveFromOperation($record)
{
    if (!$record) return null;
    $recordId = $record->record_id ?? $record['record_id'] ?? null;
    if (!$recordId) return null;

    // 只检查同一条操作记录
    $exists = BizCustomerArchive::where('source_type', '1')
        ->where('source_id', $recordId)
        ->first();
    if ($exists) return $exists;

    // ... 创建新档案 ...
}
```

#### 修改后（合并逻辑）：
```php
public function insertArchiveFromOperation($record)
{
    if (!$record) return null;
    $recordId = $record->record_id ?? $record['record_id'] ?? null;
    if (!$recordId) return null;

    $recordModel = is_array($record) ? BizOperationRecord::find($recordId) : $record;
    if (!$recordModel) return null;
    if (!$recordModel->customer_id) return null;

    // 构建新的项目数据
    $newItem = ['name' => $recordModel->product_name ?? '', 'quantity' => intval($recordModel->operation_quantity ?? 1)];
    $newAmount = $recordModel->operation_type === '1'
        ? ($recordModel->trial_price ?? 0)
        : ($recordModel->consume_amount ?? 0);

    // 处理照片
    $newPhotos = [];
    if (!empty($recordModel->before_photo)) {
        $before = json_decode($recordModel->before_photo, true);
        if (is_array($before)) $newPhotos = array_merge($newPhotos, $before);
        else $newPhotos[] = $recordModel->before_photo;
    }
    if (!empty($recordModel->after_photo)) {
        $after = json_decode($recordModel->after_photo, true);
        if (is_array($after)) $newPhotos = array_merge($newPhotos, $after);
        else $newPhotos[] = $recordModel->after_photo;
    }

    // 🔑 关键改动：按"同客户+同日期+操作类型"查找已有档案进行合并
    $archiveDate = $recordModel->operation_date ?? date('Y-m-d');
    $existing = BizCustomerArchive::where('source_type', '1')
        ->where('customer_id', $recordModel->customer_id)
        ->where('archive_date', $archiveDate)
        ->where('operator_user_id', $recordModel->operator_user_id ?? 0)
        ->first();

    if ($existing) {
        // ========== 合并到已有档案 ==========
        
        // 合并 plan_items（JSON数组追加）
        $existingItems = json_decode($existing->plan_items, true) ?: [];
        $existingItems[] = $newItem;

        // 累加金额
        $existing->amount = round(floatval($existing->amount ?? 0) + $newAmount, 2);

        // 合并照片（去重）
        $existingPhotos = json_decode($existing->photos, true) ?: [];
        $mergedPhotos = array_unique(array_merge($existingPhotos, $newPhotos));

        // 更新字段（取最新值）
        $existing->satisfaction = $recordModel->satisfaction ?? $existing->satisfaction;
        $existing->customer_feedback = $recordModel->customer_feedback ?? $existing->customer_feedback;
        if (!empty($recordModel->remark)) {
            $existing->remark = trim(($existing->remark ?? '') . '; ' . $recordModel->remark, '; ');
        }
        $existing->source_id = $recordModel->record_id;  // 更新为最新操作ID
        $existing->plan_items = json_encode($existingItems, JSON_UNESCAPED_UNICODE);
        $existing->photos = !empty($mergedPhotos) ? json_encode(array_values($mergedPhotos), JSON_UNESCAPED_UNICODE) : null;
        $existing->update_time = date('Y-m-d H:i:s');
        $existing->save();

        return $existing;
    }

    // ========== 无已有档案，创建新档案 ==========
    $data = [
        'customer_id' => $recordModel->customer_id,
        'customer_name' => $recordModel->customer_name ?? '',
        'enterprise_id' => $recordModel->enterprise_id,
        'enterprise_name' => $recordModel->enterprise_name ?? '',
        'store_id' => $recordModel->store_id,
        'store_name' => $recordModel->store_name ?? '',
        'archive_date' => $archiveDate,
        'archive_type' => $recordModel->operation_type === '1' ? 'after_sales' : 'sales',
        'source_type' => '1',
        'source_id' => $recordModel->record_id,
        'plan_items' => json_encode([$newItem], JSON_UNESCAPED_UNICODE),
        'amount' => $newAmount,
        'satisfaction' => $recordModel->satisfaction ?? null,
        'photos' => !empty($newPhotos) ? json_encode($newPhotos, JSON_UNESCAPED_UNICODE) : null,
        'customer_feedback' => $recordModel->customer_feedback ?? null,
        'operator_user_id' => $recordModel->operator_user_id ?? null,
        'operator_user_name' => $recordModel->operator_user_name ?? null,
        'remark' => $recordModel->remark ?? null,
        'create_by' => $recordModel->create_by ?? '',
        'create_time' => date('Y-m-d H:i:s')
    ];
    return BizCustomerArchive::create($data);
}
```

---

## 📊 效果对比

### 修改前
| archive_id | customer_id | archive_date | plan_items | amount |
|------------|-------------|--------------|------------|--------|
| 501 | 5 | 2026-05-09 | [{"name":"品项2","quantity":1}] | 398.00 |
| 502 | 5 | 2026-05-09 | [{"name":"品项1","quantity":1}] | 398.00 |

→ 显示为 **2条档案记录** ❌

### 修改后
| archive_id | customer_id | archive_date | plan_items | amount |
|------------|-------------|--------------|------------|--------|
| 501 | 5 | 2026-05-09 | [{"name":"品项2","quantity":1},{"name":"品项1","quantity":1}] | 796.00 |

→ 显示为 **1条合并档案记录** ✅

---

## ⚠️ 注意事项

### 影响范围
- ✅ 只影响"操作类型"的档案（`source_type='1'`）
- ✅ 不影响"开单"档案（`source_type='0'`）和"还款"档案（`source_type='2'`）
- ✅ 不影响 `biz_operation_record` 表（操作记录仍然独立保存）

### 数据兼容性
- 对历史数据无影响（旧数据保持原样）
- 新产生的操作档案自动合并
- 如果需要修复历史数据，可执行额外的SQL脚本

### 合并粒度
当前方案按以下维度合并：
- `customer_id` - 同一个客户
- `archive_date` - 同一天
- `operator_user_id` - 同一个操作人（可选）

> 如果不需要按操作人区分，可以去掉 `operator_user_id` 条件

---

## 🧪 测试验证

### 测试场景1：同日同客户操作2个项目
1. 客户A在2026-01-09操作"品项1"和"品项2"
2. 预期：产生1条档案，plan_items 包含2个项目，amount 为两者之和

### 测试场景2：同日同客户操作3个项目
1. 客户A连续操作3个不同项目
2. 预期：产生1条档案，plan_items 包含3个项目

### 测试场景3：不同日期操作
1. 客户A在2026-01-08操作1个项目
2. 客户A在2026-01-09操作1个项目
3. 预期：产生2条档案（不同天不合并）

### 测试场景4：不同客户操作
1. 客户A操作1个项目
2. 客户B操作1个项目
3. 预期：各自产生独立的档案（不跨客户合并）

---

## 📌 修改文件清单

| 文件 | 修改内容 | 改动量 |
|------|---------|--------|
| `webman/app/service/BizCustomerArchiveService.php` | 重构 `insertArchiveFromOperation()` 方法 | ~60行 |

**总计：修改1个文件，1个方法**
