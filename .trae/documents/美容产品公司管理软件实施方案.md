# 美容产品公司管理软件实施方案

## 一、技术架构

### 前端
- **框架**: RuoYi-Vue3 (Vue 3 + Element Plus)
- **端口**: 8088
- **请求前缀**: /dev-api

### 后端
- **框架**: webman (基于 Workerman)
- **数据库**: MySQL (数据库: fuchenpro, 用户名: fuchenpro, 密码: 123456)
- **端口**: 8787

## 二、数据库设计

### 1. 用户表 (admin_user)
```sql
CREATE TABLE `admin_user` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `password` varchar(255) NOT NULL COMMENT '密码',
  `name` varchar(50) NOT NULL COMMENT '姓名',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `role` tinyint NOT NULL DEFAULT 1 COMMENT '角色(1:员工, 2:主管, 3:超管)',
  `superior_id` bigint DEFAULT NULL COMMENT '上级ID',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态(0:禁用, 1:启用)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 2. 美容院品牌表 (brand)
```sql
CREATE TABLE `brand` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `name` varchar(100) NOT NULL COMMENT '品牌名称',
  `logo` varchar(255) DEFAULT NULL COMMENT 'Logo',
  `contact` varchar(50) DEFAULT NULL COMMENT '联系人',
  `phone` varchar(20) DEFAULT NULL COMMENT '联系电话',
  `address` varchar(255) DEFAULT NULL COMMENT '地址',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态(0:禁用, 1:启用)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='美容院品牌表';
```

### 3. 门店表 (store)
```sql
CREATE TABLE `store` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `brand_id` bigint NOT NULL COMMENT '品牌ID',
  `name` varchar(100) NOT NULL COMMENT '门店名称',
  `address` varchar(255) DEFAULT NULL COMMENT '地址',
  `manager_name` varchar(50) DEFAULT NULL COMMENT '负责人',
  `manager_phone` varchar(20) DEFAULT NULL COMMENT '负责人电话',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态(0:禁用, 1:启用)',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_brand_id` (`brand_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='门店表';
```

### 4. 员工排班表 (schedule)
```sql
CREATE TABLE `schedule` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` bigint NOT NULL COMMENT '员工ID',
  `store_id` bigint NOT NULL COMMENT '门店ID',
  `work_date` date NOT NULL COMMENT '工作日期',
  `start_time` time NOT NULL COMMENT '开始时间',
  `end_time` time NOT NULL COMMENT '结束时间',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态(0:取消, 1:正常)',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_store_id` (`store_id`),
  KEY `idx_work_date` (`work_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='员工排班表';
```

### 5. 客户表 (customer)
```sql
CREATE TABLE `customer` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `brand_id` bigint NOT NULL COMMENT '品牌ID',
  `store_id` bigint NOT NULL COMMENT '门店ID',
  `user_id` bigint NOT NULL COMMENT '跟进员工ID',
  `name` varchar(50) NOT NULL COMMENT '客户姓名',
  `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
  `gender` tinyint DEFAULT NULL COMMENT '性别(1:男, 2:女)',
  `age` int DEFAULT NULL COMMENT '年龄',
  `source` varchar(100) DEFAULT NULL COMMENT '来源渠道',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '客户状态(1:潜在, 2:成交, 3:流失)',
  `remark` text DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_brand_id` (`brand_id`),
  KEY `idx_store_id` (`store_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='客户表';
```

### 6. 客户档案表 (customer_file)
```sql
CREATE TABLE `customer_file` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `customer_id` bigint NOT NULL COMMENT '客户ID',
  `user_id` bigint NOT NULL COMMENT '操作员工ID',
  `brand_id` bigint NOT NULL COMMENT '品牌ID',
  `store_id` bigint NOT NULL COMMENT '门店ID',
  `before_photo` varchar(255) DEFAULT NULL COMMENT '操作前照片',
  `after_photo` varchar(255) DEFAULT NULL COMMENT '操作后照片',
  `description` text DEFAULT NULL COMMENT '操作描述',
  `result` varchar(100) DEFAULT NULL COMMENT '操作结果',
  `operate_time` datetime DEFAULT NULL COMMENT '操作时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_customer_id` (`customer_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='客户档案表';
```

### 7. 订单表 (order)
```sql
CREATE TABLE `order` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_no` varchar(50) NOT NULL COMMENT '订单号',
  `customer_id` bigint NOT NULL COMMENT '客户ID',
  `user_id` bigint NOT NULL COMMENT '跟进员工ID',
  `brand_id` bigint NOT NULL COMMENT '品牌ID',
  `store_id` bigint NOT NULL COMMENT '门店ID',
  `total_amount` decimal(10,2) NOT NULL DEFAULT 0.00 COMMENT '订单总金额',
  `paid_amount` decimal(10,2) NOT NULL DEFAULT 0.00 COMMENT '已付金额',
  `status` tinyint NOT NULL DEFAULT 1 COMMENT '状态(1:待付款, 2:部分付款, 3:已付清, 4:已取消)',
  `remark` text DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_customer_id` (`customer_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

### 8. 订单项目表 (order_item)
```sql
CREATE TABLE `order_item` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` bigint NOT NULL COMMENT '订单ID',
  `project_name` varchar(100) NOT NULL COMMENT '项目名称',
  `times` int NOT NULL DEFAULT 1 COMMENT '次数',
  `unit_price` decimal(10,2) NOT NULL DEFAULT 0.00 COMMENT '单价',
  `subtotal` decimal(10,2) NOT NULL DEFAULT 0.00 COMMENT '小计',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_order_id` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单项目表';
```

### 9. 回款记录表 (payment_record)
```sql
CREATE TABLE `payment_record` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `order_id` bigint NOT NULL COMMENT '订单ID',
  `amount` decimal(10,2) NOT NULL COMMENT '回款金额',
  `payment_method` varchar(50) DEFAULT NULL COMMENT '支付方式',
  `payment_time` datetime DEFAULT NULL COMMENT '支付时间',
  `operator_id` bigint DEFAULT NULL COMMENT '操作人ID',
  `remark` varchar(255) DEFAULT NULL COMMENT '备注',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_order_id` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='回款记录表';
```

## 三、后端文件结构 (webman)

### 目录结构
```
webman/
├── app/
│   ├── controller/
│   │   ├── AuthController.php        # 登录
│   │   ├── BrandController.php       # 品牌管理
│   │   ├── StoreController.php      # 门店管理
│   │   ├── ScheduleController.php    # 排班管理
│   │   ├── CustomerController.php    # 客户管理
│   │   ├── CustomerFileController.php # 档案管理
│   │   ├── OrderController.php       # 订单管理
│   │   └── UserController.php        # 用户管理
│   ├── model/
│   │   ├── AdminUser.php
│   │   ├── Brand.php
│   │   ├── Store.php
│   │   ├── Schedule.php
│   │   ├── Customer.php
│   │   ├── CustomerFile.php
│   │   ├── Order.php
│   │   ├── OrderItem.php
│   │   └── PaymentRecord.php
│   ├── middleware/
│   │   └── Auth.php                  # 认证中间件
│   └── route/
│       └── route.php                 # 路由配置
```

### 控制器方法

#### 1. AuthController (登录)
- `login()` - 用户登录
- `logout()` - 退出登录
- `info()` - 获取用户信息

#### 2. BrandController (品牌)
- `list()` - 品牌列表
- `create()` - 创建品牌
- `update()` - 更新品牌
- `delete()` - 删除品牌

#### 3. StoreController (门店)
- `list()` - 门店列表
- `create()` - 创建门店
- `update()` - 更新门店
- `delete()` - 删除门店

#### 4. ScheduleController (排班)
- `list()` - 排班列表
- `create()` - 创建排班
- `update()` - 更新排班
- `delete()` - 删除排班
- `mySchedule()` - 我的排班

#### 5. CustomerController (客户)
- `list()` - 客户列表
- `create()` - 创建客户
- `update()` - 更新客户
- `delete()` - 删除客户
- `detail()` - 客户详情

#### 6. CustomerFileController (档案)
- `list()` - 档案列表
- `create()` - 创建档案
- `update()` - 更新档案
- `delete()` - 删除档案
- `photos()` - 前后对比照

#### 7. OrderController (订单)
- `list()` - 订单列表
- `create()` - 创建订单
- `update()` - 更新订单
- `delete()` - 删除订单
- `detail()` - 订单详情
- `addItem()` - 添加订单项目
- `payment()` - 回款记录

#### 8. UserController (用户)
- `list()` - 用户列表
- `create()` - 创建用户
- `update()` - 更新用户
- `delete()` - 删除用户
- `subordinates()` - 下属员工

## 四、前端菜单配置

### 菜单结构
```
├── 系统管理
│   ├── 用户管理
│   └── 角色管理
├── 品牌管理
│   └── 品牌列表
├── 门店管理
│   └── 门店列表
├── 排班管理
│   ├── 我的排班
│   └── 员工排班
├── 客户管理
│   ├── 客户列表
│   └── 客户档案
└── 订单管理
    ├── 订单列表
    └── 回款记录
```

## 五、测试数据

### 用户数据
1. 超管 (admin / 123456)
2. 主管1 (manager1 / 123456)
3. 员工1 (staff1 / 123456)
4. 员工2 (staff2 / 123456)

### 品牌数据
1. 美丽庄园
2. 玫瑰人生

### 门店数据
1. 美丽庄园-旗舰店 (美丽庄园)
2. 美丽庄园-万达店 (美丽庄园)
3. 玫瑰人生-中心店 (玫瑰人生)

### 客户数据
- 5条测试客户数据

### 订单数据
- 3条测试订单数据

## 六、实施步骤

1. **创建数据库和表**
   - 创建数据库 fuchenpro
   - 执行建表SQL

2. **配置数据库连接**
   - 修改 webman config/database.php

3. **创建模型类**
   - 创建 9 个模型文件

4. **创建控制器**
   - 创建 8 个控制器文件

5. **配置路由**
   - 配置 API 路由

6. **创建认证中间件**
   - JWT 认证

7. **生成前端API文件**
   - 创建 API 调用文件

8. **生成前端页面**
   - 创建菜单和页面组件

9. **插入测试数据**
   - 用户、品牌、门店、客户、订单数据

10. **测试验证**
    - 测试各模块功能
