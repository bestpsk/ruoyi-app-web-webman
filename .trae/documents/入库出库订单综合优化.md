# 入库/出库订单显示优化计划

## 问题汇总

### 1️⃣ 入库管理状态按钮报错 ❌
**错误**: `ReferenceError: ElMessageBox is not defined`
**文件**: [stockIn/index.vue:286](front/src/views/wms/stockIn/index.vue#L286)
**原因**: `handleStatusChange` 函数使用了 `ElMessageBox`，但未在文件中导入

### 2️⃣ 出库管理总数量优化 📊
**当前问题**: 显示格式需要优化
**目标效果**:
- 无 items 数据 → 返回纯数字（如 `0`）
- 单个货品 → 显示大单位（如 `10盒`）
- 多货品同 unit → 汇总显示大单位（如 `20盒`）
- 多货品不同 unit → 只显示种类数（如 `2种`）

### 3️⃣ 查看出库单调整 🔧
**问题列表**:
- ✅ 对象类型数据未正确读取（下拉框显示"请选择"）
- ✅ 数量列：只显示数字（不显示单位）
- ✅ 规格列：移动到数量列后面（因为规格在单位类型列已显示，重复了）

### 4️⃣ 查看入库单数量调整 🔢
**问题**: 数量列应只显示数字（不显示单位），因为规格已在后面单独显示

---

## 详细修改方案

### 文件1: `d:\fuchenpro\front\src\views\wms\stockIn\index.vue`

#### 修改1.1: 导入 ElMessageBox（第253行后）

**添加导入语句**:
```javascript
import { ElMessageBox } from 'element-plus'
```

#### 修改1.2: 查看入库单 - 数量列只显示数字（第129-135行）

**当前代码**:
```vue
<el-table-column label="数量" min-width="100" align="center" header-align="center">
  <template #default="scope">
    <el-input-number v-if="!isView" v-model="scope.row.quantity" :min="1" @change="calcAmount(scope.$index)" style="width: 100%" />
    <span v-else>{{ formatItemQuantity(scope.row) }}</span>
  </template>
</el-table-column>
```

**修改为**:
```vue
<el-table-column label="数量" min-width="100" align="center" header-align="center">
  <template #default="scope">
    <el-input-number v-if="!isView" v-model="scope.row.quantity" :min="1" @change="calcAmount(scope.$index)" style="width: 100%" />
    <span v-else>{{ scope.row.originalQuantity || scope.row.quantity || 0 }}</span>
  </template>
</el-table-column>
```

---

### 文件2: `d:\fuchenpro\front\src\views\wms\stockOut\index.vue`

#### 修改2.1: 出库管理 - 优化总数量显示函数（第292-306行）

**重写 `formatQuantity` 函数**:
```javascript
function formatQuantity(row) {
  const items = row.items || []
  
  // 无 items 数据 → 返回纯数字
  if (items.length === 0) {
    return row.totalQuantity || 0
  }
  
  // 单个货品 → 显示大单位
  if (items.length === 1) {
    const item = items[0]
    const packQty = row.display_pack_qty || item.packQty || 1
    const unitLabel = getUnitLabel(row.display_unit || item.unit)
    const totalQty = row.totalQuantity || 0
    
    if (packQty > 1 && unitLabel) {
      const mainQty = totalQty / packQty
      const mainQtyStr = Number.isInteger(mainQty) ? mainQty : mainQty.toFixed(1).replace(/\.0$/, '')
      return mainQtyStr + unitLabel
    } else if (unitLabel) {
      return totalQty + unitLabel
    } else {
      return totalQty
    }
  }
  
  // 多货品：检查是否同 unit
  const firstUnit = items[0].unit
  const isSameUnit = items.every(item => item.unit === firstUnit)
  
  if (isSameUnit) {
    // 同 unit → 汇总显示大单位
    const packQty = items[0].packQty || 1
    const unitLabel = getUnitLabel(firstUnit)
    const totalQty = row.totalQuantity || 0
    
    if (packQty > 1 && unitLabel) {
      const mainQty = totalQty / packQty
      const mainQtyStr = Number.isInteger(mainQty) ? mainQty : mainQty.toFixed(1).replace(/\.0$/, '')
      return mainQtyStr + unitLabel
    } else {
      return totalQty + (unitLabel || '')
    }
  } else {
    // 不同 unit → 只显示种类数
    return items.length + '种'
  }
}
```

#### 修改2.2: 查看出库单 - 删除独立的规格列（第141-145行）

**删除整个规格列**:
```vue
<!-- 删除这部分代码 -->
<el-table-column label="规格" min-width="60" align="center" header-align="center">
  <template #default="scope">
    <span>{{ scope.row.unitType === '1' ? getUnitLabel(scope.row.unit) : getSpecLabel(scope.row.spec) }}</span>
  </template>
</el-table-column>
```

#### 修改2.3: 查看出库单 - 数量列合并显示数字+规格（第165-170行）

**修改为**:
```vue
<el-table-column label="数量" min-width="120" align="center" header-align="center">
  <template #default="scope">
    <el-input-number v-if="!isView" v-model="scope.row.quantity" :min="1" @change="calcAmount(scope.$index)" style="width: 100%" />
    <span v-else>{{ (scope.row.originalQuantity || scope.row.quantity || 0) }} {{ scope.row.unitType === '1' ? getUnitLabel(scope.row.unit) : getSpecLabel(scope.row.spec) }}</span>
  </template>
</el-table-column>
```

#### 修改2.4: 查看出库单 - 确保对象类型数据正确绑定

检查 `handleView` 函数（第378行）确保加载数据时包含 `outTargetType` 字段：

**当前代码**:
```javascript
function handleView(row) { 
  reset(); 
  getStockOut(row.stockOutId).then(response => { 
    form.value = response.data; 
    if (!form.value.items) form.value.items = []; 
    isView.value = true; 
    dialogTitle.value = "查看出库单"; 
    open.value = true 
  }) 
}
```

**验证**: 确保 API 返回的数据包含 `out_target_type` 字段，如果字段名是 snake_case，Vue 会自动转换为 camelCase

---

## 预期效果对比

### 出库管理 - 总数量列

| 场景 | 当前显示 | 修改后 |
|------|---------|--------|
| 无明细 | 0 | 0 |
| 单个货品(10支,1盒=10支) | 1盒（10支） | 1盒 |
| 多货品同单位(共20支) | 1盒（20支） | 2盒 |
| 多货品不同单位 | 1盒（15支） | 2种 |

### 查看出库单表格

| 列名 | 当前 | 修改后 |
|------|------|--------|
| 货品 | GCS-p7 | GCS-p7 |
| 规格 | 盒/支 | ❌ 删除 |
| 单位类型 | 主单位(整) | 主单位(整) |
| 换算/库存 | 1盒=10支 | 1盒=10支 |
| 数量 | 1盒 / 10支 | **1 盒** / **10 支** |
| 出货单价 | 2580/盒 | 2580/盒 |

### 查看入库单表格

| 列名 | 当前 | 修改后 |
|------|------|--------|
| 数量 | 1盒 / 10支 | **1** / **10** |
| 规格 | 盒 / 支 | 盒 / 支（保持不变）|

---

## 实施步骤顺序

1. ✅ **修复入库状态按钮报错** - 导入 ElMessageBox
2. ✅ **查看入库单数量调整** - 只显示数字
3. ✅ **出库管理总数量优化** - 重写 formatQuantity 函数
4. ✅ **查看出库单结构调整** - 删除独立规格列，数量列合并显示
5. ✅ **验证对象类型数据** - 确保 outTargetType 正确读取

## 测试要点

1. 点击入库管理状态按钮不再报错
2. 出库管理总数量根据场景正确显示
3. 查看出库单对象类型下拉框显示正确的值
4. 查看出库单数量列显示：数字 + 规格单位
5. 查看入库单数量列只显示纯数字
6. 打印预览数据与页面一致
