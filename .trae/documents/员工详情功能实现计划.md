# 员工详情功能实现计划（修订版）

## 需求分析

### 员工详情信息
- 微信号
- 生日
- 身份证
- 住址
- 入职日期
- 是否在职

### 薪资构成（独立模块）
需要支持多种薪酬架构类型：
1. **底薪** - 固定月薪
2. **销售业绩×提点** - 按销售业绩乘以提成比例
3. **回款业绩×提点** - 按回款金额乘以提成比例
4. **（回款业绩-成本）×提点** - 按利润乘以提成比例
5. **阶梯销售业绩×提点** - 按销售业绩阶梯计算提成
6. **阶梯回款业绩×提点** - 按回款业绩阶梯计算提成
7. **其他自定义架构** - 可扩展

### UI 交互
- 员工详情在用户编辑弹窗中添加详情标签页

## 数据库设计

### 1. 员工详情表 `sys_user_detail`

```sql
CREATE TABLE sys_user_detail (
  detail_id         bigint(20)      NOT NULL AUTO_INCREMENT  COMMENT '详情ID',
  user_id           bigint(20)      NOT NULL                 COMMENT '用户ID',
  wechat            varchar(50)     DEFAULT ''               COMMENT '微信号',
  birthday          date            DEFAULT NULL             COMMENT '生日',
  id_card           varchar(18)     DEFAULT ''               COMMENT '身份证号',
  address           varchar(200)    DEFAULT ''               COMMENT '住址',
  hire_date         date            DEFAULT NULL             COMMENT '入职日期',
  employment_status char(1)         DEFAULT '0'              COMMENT '在职状态（0在职 1离职）',
  resign_date       date            DEFAULT NULL             COMMENT '离职日期',
  create_by         varchar(64)     DEFAULT ''               COMMENT '创建者',
  create_time       datetime                                 COMMENT '创建时间',
  update_by         varchar(64)     DEFAULT ''               COMMENT '更新者',
  update_time       datetime                                 COMMENT '更新时间',
  remark            varchar(500)    DEFAULT NULL             COMMENT '备注',
  PRIMARY KEY (detail_id),
  UNIQUE KEY uk_user_id (user_id)
) ENGINE=InnoDB COMMENT='员工详情表';
```

### 2. 薪资架构类型表 `hr_salary_type`

定义不同的薪资计算方式：

```sql
CREATE TABLE hr_salary_type (
  type_id           bigint(20)      NOT NULL AUTO_INCREMENT  COMMENT '类型ID',
  type_code         varchar(50)     NOT NULL                 COMMENT '类型编码',
  type_name         varchar(100)    NOT NULL                 COMMENT '类型名称',
  calc_formula      varchar(500)    DEFAULT ''               COMMENT '计算公式说明',
  status            char(1)         DEFAULT '0'              COMMENT '状态（0正常 1停用）',
  create_by         varchar(64)     DEFAULT ''               COMMENT '创建者',
  create_time       datetime                                 COMMENT '创建时间',
  update_by         varchar(64)     DEFAULT ''               COMMENT '更新者',
  update_time       datetime                                 COMMENT '更新时间',
  remark            varchar(500)    DEFAULT NULL             COMMENT '备注',
  PRIMARY KEY (type_id),
  UNIQUE KEY uk_type_code (type_code)
) ENGINE=InnoDB COMMENT='薪资架构类型表';

-- 初始化薪资类型数据
INSERT INTO hr_salary_type (type_code, type_name, calc_formula, status, create_by, create_time, remark) VALUES
('BASE_SALARY', '底薪', '固定金额', '0', 'admin', NOW(), '每月固定发放'),
('SALES_COMMISSION', '销售业绩提成', '销售业绩 × 提成比例', '0', 'admin', NOW(), '按销售业绩计算'),
('PAYMENT_COMMISSION', '回款业绩提成', '回款金额 × 提成比例', '0', 'admin', NOW(), '按回款金额计算'),
('PROFIT_COMMISSION', '利润提成', '(回款金额 - 成本) × 提成比例', '0', 'admin', NOW(), '按利润计算'),
('TIERED_SALES', '阶梯销售提成', '按销售业绩阶梯计算提成', '0', 'admin', NOW(), '阶梯式销售提成'),
('TIERED_PAYMENT', '阶梯回款提成', '按回款业绩阶梯计算提成', '0', 'admin', NOW(), '阶梯式回款提成');
```

### 3. 用户薪资配置表 `hr_user_salary`

记录每个员工的薪资构成配置：

```sql
CREATE TABLE hr_user_salary (
  salary_id         bigint(20)      NOT NULL AUTO_INCREMENT  COMMENT '薪资配置ID',
  user_id           bigint(20)      NOT NULL                 COMMENT '用户ID',
  type_id           bigint(20)      NOT NULL                 COMMENT '薪资类型ID',
  base_amount       decimal(12,2)   DEFAULT 0.00             COMMENT '基础金额/底薪',
  commission_rate   decimal(5,4)    DEFAULT 0.0000           COMMENT '提成比例（如0.05表示5%）',
  tier_config       text            DEFAULT NULL             COMMENT '阶梯配置（JSON格式）',
  effective_date    date            DEFAULT NULL             COMMENT '生效日期',
  expire_date       date            DEFAULT NULL             COMMENT '失效日期',
  status            char(1)         DEFAULT '0'              COMMENT '状态（0正常 1停用）',
  create_by         varchar(64)     DEFAULT ''               COMMENT '创建者',
  create_time       datetime                                 COMMENT '创建时间',
  update_by         varchar(64)     DEFAULT ''               COMMENT '更新者',
  update_time       datetime                                 COMMENT '更新时间',
  remark            varchar(500)    DEFAULT NULL             COMMENT '备注',
  PRIMARY KEY (salary_id),
  KEY idx_user_id (user_id),
  KEY idx_type_id (type_id)
) ENGINE=InnoDB COMMENT='用户薪资配置表';
```

**阶梯配置 JSON 示例**：
```json
[
  {"min": 0, "max": 100000, "rate": 0.03},
  {"min": 100000, "max": 500000, "rate": 0.05},
  {"min": 500000, "max": null, "rate": 0.08}
]
```

### 4. 薪资架构阶梯表 `hr_salary_tier`（可选，用于复杂阶梯配置）

如果阶梯配置较复杂，可单独建表：

```sql
CREATE TABLE hr_salary_tier (
  tier_id           bigint(20)      NOT NULL AUTO_INCREMENT  COMMENT '阶梯ID',
  salary_id         bigint(20)      NOT NULL                 COMMENT '薪资配置ID',
  tier_level        int             DEFAULT 1                COMMENT '阶梯级别',
  min_amount        decimal(12,2)   DEFAULT 0.00             COMMENT '最小金额',
  max_amount        decimal(12,2)   DEFAULT NULL             COMMENT '最大金额（NULL表示无上限）',
  commission_rate   decimal(5,4)    DEFAULT 0.0000           COMMENT '提成比例',
  create_time       datetime                                 COMMENT '创建时间',
  PRIMARY KEY (tier_id),
  KEY idx_salary_id (salary_id)
) ENGINE=InnoDB COMMENT='薪资阶梯配置表';
```

## 后端实现

### 文件结构

```
webman/app/
├── model/
│   ├── SysUserDetail.php          # 员工详情模型
│   ├── HrSalaryType.php           # 薪资类型模型
│   ├── HrUserSalary.php           # 用户薪资配置模型
│   └── SysUser.php                # 修改：添加关联
├── service/
│   ├── SysUserDetailService.php   # 员工详情服务
│   └── HrUserSalaryService.php    # 薪资配置服务
└── controller/
    └── system/
        ├── SysUserDetailController.php   # 员工详情控制器
        └── HrUserSalaryController.php    # 薪资配置控制器
```

### API 接口设计

#### 员工详情接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/system/user/detail/{userId}` | 获取员工详情 |
| POST | `/system/user/detail` | 新增员工详情 |
| PUT | `/system/user/detail` | 修改员工详情 |

#### 薪资配置接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/hr/salary/type/list` | 获取薪资类型列表 |
| GET | `/hr/salary/user/{userId}` | 获取用户薪资配置列表 |
| POST | `/hr/salary/user` | 新增用户薪资配置 |
| PUT | `/hr/salary/user` | 修改用户薪资配置 |
| DELETE | `/hr/salary/user/{salaryIds}` | 删除用户薪资配置 |

## 前端实现

### 文件结构

```
front/src/
├── api/
│   └── system/
│       └── user.js                # 修改：添加员工详情和薪资接口
└── views/
    └── system/
        └── user/
            ├── index.vue          # 修改：用户列表页
            └── components/
                ├── UserDetail.vue     # 新增：员工详情标签页组件
                └── UserSalary.vue     # 新增：薪资配置标签页组件
```

### UI 设计

用户编辑弹窗使用 Tabs 标签页：
1. **基本信息** - 原有的用户信息编辑
2. **员工详情** - 微信号、生日、身份证、住址、入职日期、是否在职
3. **薪资配置** - 薪资构成列表，支持添加多种薪资类型

## 实现步骤

### 第一步：数据库变更
1. 创建 `sys_user_detail` 表
2. 创建 `hr_salary_type` 表并初始化数据
3. 创建 `hr_user_salary` 表
4. 创建 `hr_salary_tier` 表（可选）

### 第二步：后端开发
1. 创建模型文件
2. 创建服务类
3. 创建控制器
4. 修改 SysUser 模型添加关联

### 第三步：前端开发
1. 添加 API 接口
2. 创建员工详情组件
3. 创建薪资配置组件
4. 修改用户编辑弹窗，添加标签页

### 第四步：权限配置
1. 添加员工详情相关菜单权限
2. 添加薪资配置相关菜单权限

## 文件变更清单

| 类型 | 文件路径 | 操作 |
|------|----------|------|
| SQL | `webman/sql/hr_tables.sql` | 新建 |
| Model | `webman/app/model/SysUserDetail.php` | 新建 |
| Model | `webman/app/model/HrSalaryType.php` | 新建 |
| Model | `webman/app/model/HrUserSalary.php` | 新建 |
| Model | `webman/app/model/HrSalaryTier.php` | 新建（可选） |
| Model | `webman/app/model/SysUser.php` | 修改 |
| Service | `webman/app/service/SysUserDetailService.php` | 新建 |
| Service | `webman/app/service/HrUserSalaryService.php` | 新建 |
| Controller | `webman/app/controller/system/SysUserDetailController.php` | 新建 |
| Controller | `webman/app/controller/system/HrUserSalaryController.php` | 新建 |
| Vue | `front/src/views/system/user/components/UserDetail.vue` | 新建 |
| Vue | `front/src/views/system/user/components/UserSalary.vue` | 新建 |
| API | `front/src/api/system/user.js` | 修改 |
| Vue | `front/src/views/system/user/index.vue` | 修改 |

## 薪资计算说明

### 各类型计算方式

| 类型 | 计算公式 | 参数说明 |
|------|----------|----------|
| 底薪 | base_amount | 固定金额 |
| 销售业绩提成 | 销售业绩 × commission_rate | 提成比例 |
| 回款业绩提成 | 回款金额 × commission_rate | 提成比例 |
| 利润提成 | (回款金额 - 成本) × commission_rate | 提成比例 |
| 阶梯销售提成 | 按阶梯配置计算 | tier_config JSON |
| 阶梯回款提成 | 按阶梯配置计算 | tier_config JSON |

### 阶梯计算示例

假设阶梯配置：
```json
[
  {"min": 0, "max": 100000, "rate": 0.03},
  {"min": 100000, "max": 500000, "rate": 0.05},
  {"min": 500000, "max": null, "rate": 0.08}
]
```

若销售业绩为 600,000：
- 0 ~ 100,000：100,000 × 3% = 3,000
- 100,000 ~ 500,000：400,000 × 5% = 20,000
- 500,000 ~ 600,000：100,000 × 8% = 8,000
- **总计：31,000**
