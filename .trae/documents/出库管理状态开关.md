# 出库管理 - 状态列改为开关（取消确认按钮）

## 需求描述

1. **状态列**：从文字标签改为 **el-switch 开关**（类似入库管理）
2. **操作列**：移除"确认"按钮，因为审核功能已整合到状态开关中
3. **功能**：通过开关直接审核/取消审核出库单

## 当前状态 vs 目标效果

### 当前
```
| 出库单号 | ... | 状态    | 操作                    |
| CK...003 | ... | 待确认   | 👁查看 ✏修改 ✓确认 🗑删除 |
| CK...001 | ... | 已确认   | 👁查看                   |
```

### 目标
```
| 出库单号 | ... | 状态        | 操作          |
| CK...003 | ... | [⚪ 开关]   | 👁查看 ✏修改 🗑删除  |
| CK...001 | ... | [🔵 开关]   | 👁查看          |
```

---

## 详细修改步骤

### 文件: `d:\fuchenpro\front\src\views\wms\stockOut\index.vue`

#### 修改1: 状态列 - 改为 el-switch

**当前代码** (查找 `label="状态"` 的列):
```vue
<el-table-column label="状态" prop="status" min-width="65" align="center">
  <template #default="scope">
    <el-tag :type="scope.row.status === '1' ? '' : 'warning'">
      {{ scope.row.status === '1' ? '已确认' : '待确认' }}
    </el-tag>
  </template>
</el-table-column>
```

**修改为**:
```vue
<el-table-column label="状态" prop="status" min-width="80" align="center">
  <template #default="scope">
    <el-switch 
      v-model="scope.row.status" 
      active-value="1" 
      inactive-value="0"
      @change="(val) => handleStatusChange(scope.row, val)" 
    />
  </template>
</el-table-column>
```

#### 修改2: 操作列 - 移除确认按钮

**当前代码**:
```vue
<el-table-column label="操作" width="180" align="center">
  <template #default="scope">
    <el-button link type="primary" icon="View" @click="handleView(scope.row)">查看</el-button>
    <el-button v-if="scope.row.status === '0'" link type="primary" icon="Edit" @click="handleUpdate(scope.row)">修改</el-button>
    <el-button v-if="scope.row.status === '0'" link type="success" icon="Check" @click="handleConfirm(scope.row)">确认</el-button>
    <el-button link type="danger" icon="Delete" @click="handleDelete(scope.row)">删除</el-button>
  </template>
</el-table-column>
```

**修改为**:
```vue
<el-table-column label="操作" width="150" align="center">
  <template #default="scope">
    <el-button link type="primary" icon="View" @click="handleView(scope.row)">查看</el-button>
    <el-button v-if="scope.row.status === '0'" link type="primary" icon="Edit" @click="handleUpdate(scope.row)">修改</el-button>
    <el-button link type="danger" icon="Delete" @click="handleDelete(scope.row)">删除</el-button>
  </template>
</el-table-column>
```

#### 修改3: 添加 handleStatusChange 函数

在 script 部分添加：

```javascript
import { ElMessageBox } from 'element-plus'
import { confirmStockOutById, cancelConfirmStockOut } from "@/api/wms/stockOut"

function handleStatusChange(row, val) {
  const action = val === '1' ? '确认出库' : '取消确认'
  ElMessageBox.confirm(`确定要${action}该出库单吗？${val === '1' ? '\n确认后将扣减库存！' : '\n取消后将恢复库存！'}`, '提示', {
    confirmButtonText: '确定',
    cancelButtonText: '取消',
    type: 'warning'
  }).then(() => {
    if (val === '1') {
      confirmStockOutById(row.stockOutId).then(() => {
        proxy.$modal.msgSuccess('出库确认成功')
        getList()
      }).catch(() => {
        row.status = '0'
        proxy.$modal.msgError('操作失败')
      })
    } else {
      cancelConfirmStockOut(row.stockOutId).then(() => {
        proxy.$modal.msgSuccess('已取消确认')
        getList()
      }).catch(() => {
        row.status = '1'
        proxy.$modal.msgError('操作失败')
      })
    }
  }).catch(() => {
    row.status = val === '1' ? '0' : '1'
  })
}
```

#### 修改4: 导入必要的 API 和组件

确保 import 语句包含：
```javascript
import { ElMessageBox } from 'element-plus'
import { listStockOut, getStockOut, delStockOut, addStockOut, updateStockOut, confirmStockOut, confirmStockOutById, cancelConfirmStockOut } from "@/api/wms/stockOut"
```

---

### 文件: `d:\fuchenpro\front\src\api\wms\stockOut.js` (如需要)

检查是否已有以下 API 方法，如果没有则添加：

```javascript
export function confirmStockOutById(stockOutId) {
  return request({ url: '/wms/stockOut/confirm/' + stockOutId, method: 'put' })
}

export function cancelConfirmStockOut(stockOutId) {
  return request({ url: '/wms/stockOut/cancelConfirm/' + stockOutId, method: 'put' })
}
```

---

### 后端文件（如果需要）

检查是否已有：
- `/wms/stockOut/confirm/{id}` - PUT 方法（确认出库）
- `/wms/stockOut/cancelConfirm/{id}` - PUT 方法（取消确认）

---

## 预期效果对比

### 修改前
- ❌ 状态显示文字标签（待确认/已确认）
- ❌ 操作列有"确认"按钮
- ❌ 需要点击确认按钮才能审核

### 修改后
- ✅ 状态显示开关控件
  - 关闭(⚪) = 待确认
  - 打开(🔵) = 已确认
- ✅ 操作列无"确认"按钮
- ✅ 直接切换开关即可审核/取消审核
- ✅ 切换时弹出确认对话框
- ✅ 确认出库时提示"将扣减库存"
- ✅ 取消确认时提示"将恢复库存"

---

## 测试要点

1. ✅ 待状态的出库单显示关闭的开关
2. ✅ 已确认的出库单显示打开的开关
3. ✅ 点击开关弹出确认对话框
4. ✅ 确认后状态改变，库存正确变化
5. ✅ 取消操作恢复原状态
6. ✅ 待确认状态下可修改、可删除
7. ✅ 已确认状态下只能查看和取消确认
