# 服务监控功能完善计划

## 一、需求分析

基于用户需求，需要完善服务监控页面和功能：
1. 将"Java虚拟机信息"改为"Webman信息"
2. 借鉴RuoYi-Vue-fast-master项目的实现方式
3. 完善CPU、内存、磁盘等监控数据

## 二、当前实现分析

### 当前后端实现 (ServerController.php)
- CPU：仅获取核心数，缺少用户使用率、系统使用率、空闲率
- 内存：仅获取总内存、已用内存、剩余内存，缺少使用率
- JVM：使用PHP内存模拟，需要改为Webman相关信息
- 磁盘：仅获取当前目录所在磁盘，需要获取所有磁盘分区

### 当前前端实现 (index.vue)
- 显示"Java虚拟机信息"标题
- 显示Java名称、Java版本、启动时间、运行时长、安装路径、项目路径、运行参数等字段

## 三、改进方案

### 3.1 后端改进 (ServerController.php)

#### 3.1.1 CPU信息完善
- 核心数：已有
- 用户使用率：需要添加（Linux通过/proc/stat计算）
- 系统使用率：需要添加（Linux通过/proc/stat计算）
- 当前空闲率：需要添加（Linux通过/proc/stat计算）

#### 3.1.2 内存信息完善
- 总内存：已有
- 已用内存：已有
- 剩余内存：已有
- 使用率：需要添加计算

#### 3.1.3 Webman信息（替换JVM）
- PHP版本：PHP_VERSION
- Webman版本：从composer.json获取
- 启动时间：记录服务启动时间
- 运行时长：计算当前时间与启动时间的差值
- 安装路径：PHP_BINARY
- 项目路径：root_path()
- 运行参数：$_SERVER['argv'] 或从启动命令获取
- 内存限制：ini_get('memory_limit')
- 最大执行时间：ini_get('max_execution_time')

#### 3.1.4 磁盘信息完善
- 获取所有磁盘分区（Windows获取所有盘符，Linux获取所有挂载点）

### 3.2 前端改进 (index.vue)

#### 3.2.1 标题修改
- 将"Java虚拟机信息"改为"Webman信息"

#### 3.2.2 字段调整
- Java名称 → PHP版本
- Java版本 → Webman版本
- 启动时间：保持
- 运行时长：保持
- 安装路径：保持（改为PHP安装路径）
- 项目路径：保持
- 运行参数：保持

#### 3.2.3 内存表格调整
- 将"JVM"列改为"Webman"列

## 四、具体实现步骤

### 步骤1：修改后端控制器
文件：`d:\fuchenpro\webman\app\controller\monitor\ServerController.php`

1. 添加CPU使用率计算方法
2. 添加内存使用率计算
3. 添加Webman信息获取方法
4. 完善磁盘信息获取方法

### 步骤2：修改前端页面
文件：`d:\fuchenpro\front\src\views\monitor\server\index.vue`

1. 修改"Java虚拟机信息"标题为"Webman信息"
2. 调整字段显示
3. 修改内存表格中的"JVM"列为"Webman"

### 步骤3：测试验证
1. 启动Webman服务
2. 访问服务监控页面
3. 验证各项数据显示正确

## 五、技术细节

### 5.1 CPU使用率计算（Linux）
```php
// 读取 /proc/stat 获取CPU使用情况
// 计算公式：
// total = user + nice + system + idle + iowait + irq + softirq
// usage = (total - idle) / total * 100
```

### 5.2 Webman版本获取
```php
// 从 composer.json 读取
$composer = json_decode(file_get_contents(root_path() . 'composer.json'), true);
$version = $composer['require']['workerman/webman-framework'] ?? 'unknown';
```

### 5.3 启动时间记录
```php
// 在服务启动时记录时间
// 可以使用常量或静态变量存储
define('WEBMAN_START_TIME', time());
```

### 5.4 磁盘信息获取
```php
// Windows: 使用 wmic 或遍历 C-Z 盘符
// Linux: 读取 /proc/mounts 或使用 df 命令
```

## 六、预期效果

完成后，服务监控页面将显示：
1. CPU信息：核心数、用户使用率、系统使用率、空闲率
2. 内存信息：总内存、已用内存、剩余内存、使用率（系统和Webman）
3. 服务器信息：服务器名称、IP、操作系统、系统架构
4. Webman信息：PHP版本、Webman版本、启动时间、运行时长、安装路径、项目路径、运行参数
5. 磁盘状态：所有磁盘分区的详细信息
